# Mini DEX Frontend

Next.js + TailwindCSS frontend for the Mini DEX.

## Setup
1. Copy `.env.example` to `.env` and set values.
2. Install dependencies: `npm install`
3. Run dev server: `npm run dev`
