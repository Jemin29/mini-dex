# Mini DEX Backend

Production-grade API + WebSocket layer for indexing, analytics, and user activity.

## Scripts
- `npm run dev`
- `npm run build`
- `npm start`

## Env
Copy `.env.example` to `.env` and update values.
